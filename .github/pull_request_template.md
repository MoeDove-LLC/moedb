## 变更

- 对象及操作（新增/修改/删除）：
- 原因：
- 相关 ASN/前缀及关系说明：

## 确认

- [ ] 本 PR 只修改 `data/`，所有修改前、修改后的对象都只涉及同一个联系人。
- [ ] 若包含其他对象，其 `admin-c` 和 `tech-c` 都指向该联系人；若删除联系人，最终仓库中已无引用。
- [ ] source、文件名和 `changed` 日期符合投稿指南。
- [ ] 未提交 `mnt-by:`、任何密码、token、私钥或 `auth:` / `delete:` 内容。
- [ ] 我理解检查通过不代表管理员必须合并或发布。
